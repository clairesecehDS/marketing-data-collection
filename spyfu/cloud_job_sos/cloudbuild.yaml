steps:
  # Build l'image Docker
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - '${_IMAGE_NAME}'
      - '-f'
      - 'cloud_job_sos/Dockerfile'
      - '.'

images:
  - '${_IMAGE_NAME}'

substitutions:
  _IMAGE_NAME: 'gcr.io/international-sos-479209/spyfu-sos-collection'

timeout: '1200s'
